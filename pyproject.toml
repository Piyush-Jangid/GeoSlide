[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "GeoSlide"
authors = [{name = "sumeetksinha", email = "sksinha@civil.iitd.ac.in"}]
dynamic = ["version", "description"]
